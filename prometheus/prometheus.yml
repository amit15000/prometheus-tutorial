global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  # Docker Compose DNS: service name `app` on the default network
  - job_name: prometheus-tutorial-app
    metrics_path: /metrics
    static_configs:
      - targets:
          - app:3000
        labels:
          service: tutorial-app
