# Loads JSON dashboards from the mounted folder on startup
apiVersion: 1

providers:
  - name: tutorial-dashboards
    orgId: 1
    folder: Tutorial
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
